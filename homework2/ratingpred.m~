% Input: number of labels k
%
%vector theta of d rows, 1 column
%
%vector b of k-1 rows, 1 column
%
%vector x of d rows, 1 column
% Output: label (1 or 2 ... or k)
function label = ratingpred(k,theta,b,x)
n = size(x, 2);
for i = 1:n
    d = dot(theta, x.')
    disp
    if i == n-1
        if b(i-1) < d
            label = i
        end
    elseif i == 1
        if d <= b(i)
            label = i
        end
    elseif b(i-1) < d && d <= b(i)
        label = i
    end

    end
end